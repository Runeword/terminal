---
paths:
  - "**/*.{jsx,tsx}"
---

# React Development Rules

## Firefox MCP

When debugging UI issues, visual bugs, or runtime behavior, use the firefox-devtools MCP tools to navigate to the running app (localhost:3000), take screenshots, inspect the DOM, and check console/network output before suggesting fixes.

% aws, lambda

# List functions
aws lambda list-functions

# List functions
aws lambda list-functions --profile <profile>

# List profiles
aws configure list-profiles

# Build application
sam build

# Run local
sam local start-api

# Run local
sam local start-api --profile <profile> 

# Create application
sam init

# Create application
sam init --runtime go --package-type Image
